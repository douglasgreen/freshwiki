- When you browse the hierarchy, you should only be searching within that node.
- Browsing nodes shows a list of nodes, pages, and files within that node. Searching within the list
  changes the view to include matching nodes, pages, and files in the entire subtree of that node.
